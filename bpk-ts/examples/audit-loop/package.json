{
  "name": "@batpak/example-audit-loop",
  "version": "0.0.0",
  "private": true,
  "description": "Demo-owned loop: commit app events and rebuild a view from event.query + event.get.",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run --root .",
    "start": "node ./dist/index.js",
    "clean": "node -e \"for (const p of ['dist','tsconfig.tsbuildinfo']) require('node:fs').rmSync(p,{recursive:true,force:true})\""
  },
  "dependencies": {
    "@batpak/sdk": "workspace:*"
  },
  "devDependencies": {
    "vitest": "3.2.6"
  }
}
