{
  "name": "batpak-ts-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "BatPAK TypeScript SDK workspace. Phase 0: NETBAT/1 wire-parity slice.",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "generate": "pnpm --filter @batpak/codegen run generate",
    "clean": "pnpm -r run clean"
  },
  "devDependencies": {
    "typescript": "5.6.3",
    "vitest": "2.1.9",
    "@types/node": "22.10.5"
  },
  "packageManager": "pnpm@10.33.0"
}
