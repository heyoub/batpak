{
  "name": "@batpak/schema",
  "version": "0.0.0",
  "private": true,
  "description": "Phase 0 stub: Effect Schema v4 beta surface for future TS authoring. NOT source-of-truth in Phase 0.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run --root .",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "effect": "4.0.0-beta.68"
  }
}
