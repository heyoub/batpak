# Minimal `batpak` app

This template is copyable. Inside this repo it uses a path dependency to the root crate; in your own project you can switch that to a crates.io version.

Run:

```bash
cargo run
```
