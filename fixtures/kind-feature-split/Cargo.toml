[package]
name = "batpak-kind-feature-split"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]

[features]
extra-payload = []

[dependencies]
batpak = { path = "../.." }
serde = { version = "1", features = ["derive"] }
